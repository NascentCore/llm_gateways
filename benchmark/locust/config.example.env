# 压测客户端环境变量示例（复制为 .env 或 export 后使用）
# NewAPI 地址（第三台机器需填第一台机器的内网 IP 或 LB 地址）
NEWAPI_BASE_URL=http://NEWAPI_HOST:3000
# 在 NewAPI 后台创建的 Token
NEWAPI_API_KEY=sk-xxx
# 与 NewAPI 中配置的、指向 Mock 的模型名一致
NEWAPI_MODEL=mock-model
# 单次 HTTP 请求超时（秒）；大 input/output case 需要足够长，默认 300
LOCUST_REQUEST_TIMEOUT=300
